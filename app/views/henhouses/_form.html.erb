<%= form_for(henhouse) do |f| %>
  <% if henhouse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(henhouse.errors.count, "error") %> prohibited this henhouse from being saved:</h2>

      <ul>
      <% henhouse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no %>
    <%= f.text_field :no %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :capacity %>
    <%= f.number_field :capacity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
