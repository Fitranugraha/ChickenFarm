<%= form_for(periode) do |f| %>
  <% if periode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(periode.errors.count, "error") %> prohibited this periode from being saved:</h2>

      <ul>
      <% periode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no %>
    <%= f.text_field :no %>
  </div>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.date_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.date_select :end_date %>
  </div>

  <div class="field">
    <%= f.label :total_do %>
    <%= f.number_field :total_do %>
  </div>
  

	
  <div class="field"> 
    <%= f.label :henhouse_id %>
	<%= collection_select(:periode, :henhouse_id, Henhouse.all, :id, :name) %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
